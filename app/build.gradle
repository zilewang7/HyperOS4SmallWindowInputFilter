plugins {
    id "com.android.application"
}

android {
    namespace "dev.codex.smallwindow"
    compileSdk 37

    defaultConfig {
        applicationId "dev.codex.smallwindow"
        minSdk 36
        targetSdk 36
        versionCode 2
        versionName "1.0.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    compileOnly "io.github.libxposed:api:102.0.0"
}

dependencies {
    compileOnly files("stubs/inputfilter-stubs.jar")
}
